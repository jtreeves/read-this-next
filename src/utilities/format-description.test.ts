import { formatDescription } from './format-description'

describe('formatDescription utility', () => {
    it('should be a function', () => {
        expect(typeof formatDescription).toBe('function')
    })
})
