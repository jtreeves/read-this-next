import { createFetchOptions } from './create-fetch-options'

describe('createFetchOptions utility', () => {
    it('should be a function', () => {
        expect(typeof createFetchOptions).toBe('function')
    })
})
