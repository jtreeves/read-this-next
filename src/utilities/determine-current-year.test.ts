import { determineCurrentYear } from './determine-current-year'

describe('determineCurrentYear utility', () => {
    it('should be a function', () => {
        expect(typeof determineCurrentYear).toBe('function')
    })
})
