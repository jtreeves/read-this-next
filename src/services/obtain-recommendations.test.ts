import { obtainRecommendations } from './obtain-recommendations'

describe('obtainRecommendations utility', () => {
    it('should be a function', () => {
        expect(typeof obtainRecommendations).toBe('function')
    })
})
