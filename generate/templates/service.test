import { {{SERVICE_NAME}} } from './{{FILE_NAME}}'

describe('{{SERVICE_NAME}} utility', () => {
    it('should be a function', () => {
        expect(typeof {{SERVICE_NAME}}).toBe('function')
    })
})
